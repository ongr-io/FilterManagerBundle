sudo: false
language: php
php:
  - 5.5
  - 5.6
  - 7.0
  - hhvm
services:
  - elasticsearch
matrix:
  allow_failures:
    - php: hhvm
before_script:
  - composer config -g github-oauth.github.com $GITHUB_COMPOSER_AUTH
  - composer global require squizlabs/php_codesniffer --no-interaction
  - composer global require satooshi/php-coveralls --no-interaction
  - export PATH="$HOME/.composer/vendor/bin:$PATH"
  - composer install --no-interaction
script:
  - vendor/bin/phpunit --coverage-clover=coveralls.clover
  - phpcs -p --standard=PSR2 --ignore=vendor/,Tests/app/ ./
after_script:
  - coveralls
notifications:
  webhooks:
    urls:
      - https://webhooks.gitter.im/e/81e47479feba5ba8dc47
    on_success: change
    on_start: never
